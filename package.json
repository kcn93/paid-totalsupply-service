{
  "name": "slots-faucet-function",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "gcloud functions deploy token-v3-total-supply --env-vars-file .env --entry-point v3totalSupply --runtime nodejs8 --trigger-http",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "thorify": "1.1.0",
    "web3": "1.0.0-beta.37",
    "@decent-bet/contract-migration": "1.0.1"
  }
}
